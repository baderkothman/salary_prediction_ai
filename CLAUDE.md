# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository state

Implementation is underway, phase by phase, driven by a master prompt that adapted the plan below to a `frontend/` / `backend/` / `ml/` / `scripts/` layout (see "Actual repository layout"). Before writing any new code, read the six spec files in this order (this is also the mandated order in `plan.md` §1):

1. `prd.md` — product requirements, functional requirements (FR-01…FR-12), acceptance criteria.
2. `architecture.md` — system design, component responsibilities, repo layout, API/DB contracts, architectural invariants.
3. `essentials.md` — condensed fast-start contract; if it conflicts with another doc on a minor point, treat it as the practical summary but defer to `architecture.md`/`prd.md` for authoritative detail.
4. `security.md` — trust boundaries, secret handling, RLS policy requirements.
5. `design.md` — UI direction, layout, tokens, component list for the React app.
6. `plan.md` — the phased execution plan (Phase 0 through Phase 16) with exit criteria per phase. Follow phases in order; do not build later-phase features early.

`plan.md` also requires inspecting the actual downloaded dataset before hardcoding any column/schema assumptions, and maintaining a task checklist in the repo as work proceeds.

## What this project is

An end-to-end ML salary-prediction system for data-science jobs (Kaggle `ruchi798/data-science-job-salaries`), split into two deliberately separate halves:

- **Local generation pipeline** (not deployed): cleans data, trains a `DecisionTreeRegressor` via scikit-learn `Pipeline`, calls a local FastAPI instance to get predictions for every distinct observed feature tuple, builds deterministic comparison statistics in Python, sends those facts to a **local Ollama** model to get a structured narrative + chart spec, validates that output, and publishes it to Supabase.
- **Deployed consumption layer**: a React/Vite/TypeScript dashboard that reads only from Supabase (published runs), plus an **independently deployed FastAPI** instance serving the same model artifact for direct API evaluation.

The React dashboard never calls Ollama, the local pipeline, or the FastAPI predict endpoint at runtime — it only reads persisted, published data from Supabase.

## Architectural invariants (do not violate without documenting the change — `architecture.md` §12)

1. Production model is a `DecisionTreeRegressor` (not swapped for another algorithm in production).
2. Prediction API is **GET**-based with query params (assignment requirement), not POST.
3. All API inputs are validated via Pydantic/FastAPI types against model metadata.
4. Preprocessing and the model are serialized together in one scikit-learn `Pipeline` so they can never drift apart.
5. Ollama runs locally only, used during pre-generation — never called live from the deployed dashboard.
6. Dashboard data is always persisted (to Supabase) before it is displayed.
7. React reads from Supabase only; it does not generate analysis live.
8. The deployed FastAPI service is an independent deliverable, functional even if the dashboard is down.
9. A `pipeline_runs` row starts as `building` and is only flipped to `published` after completeness checks pass — partial runs must never be exposed to the dashboard.
10. LLM output is untrusted structured data: validate with Pydantic before persistence, validate again with Zod on the frontend before rendering; never execute LLM-generated code and never render it as raw HTML.

## Actual repository layout

This supersedes `architecture.md` §4's `apps/`/`pipeline/`/top-level `data/` naming — the master implementation prompt asked for this structure instead, and there was no strong technical reason to refuse it. The responsibilities from `architecture.md` map onto it 1:1:

```text
frontend/                    React + Vite + TypeScript dashboard (scaffolded via `npm create vite -- --template react-ts`)
backend/
  app/{api,core,schemas,services,repositories}/  FastAPI service — loads the joblib pipeline, no ml/ or Supabase imports
  requirements.txt           Minimal deployable dependency set (independent of ml/dev extras)
ml/
  data/{raw,processed}/      raw is immutable; processed is regenerated by the cleaning script
  src/{data,features,training,evaluation}/
  artifacts/{model,reports}/ salary_model.joblib, model_metadata.json, data_profile.json, cleaning_report.json
  notebooks/, tests/
scripts/                     Local generation pipeline orchestration — this is architecture.md's "pipeline/" renamed:
                              generate_inputs.py, api_client.py, build_context.py, llm_client.py,
                              validate_analysis.py, persist.py, run_pipeline.py
docs/                        Generated reports (dataset inspection, cleaning, evaluation) — the six spec .md files stay at repo root
supabase/migrations/         (created in Phase 9)
requirements-dev.txt         Full dev environment: ml + backend + scripts combined, one shared .venv
```

Do not duplicate preprocessing logic across `ml/`, `scripts/`, and `backend/` — the serialized `Pipeline` artifact (`ml/artifacts/model/salary_model.joblib`) is the single source of truth for feature transforms; `backend/` only deserializes it and never reimplements a transform.

Small dataset/model artifacts (`ml/data/raw`, `ml/data/processed`, `ml/artifacts/`) are committed to git rather than ignored — the dataset is small (~600 rows) and committing the trained artifact lets the independently-deployed FastAPI service deploy without a training step in CI. `.env` and `node_modules`/`.venv` remain gitignored per `security.md`.

## Commands

```bash
# Python env (one shared venv for ml + backend + scripts)
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements-dev.txt

uvicorn backend.app.main:app --reload --port 8000     # local API (routes land in Phase 6)
pytest                                                 # Python tests

# Frontend
cd frontend && npm install && npm run dev && npm run build
```

Commands for `ml.src.training.train`, `scripts/run_pipeline.py`, etc. will be added here as those phases land.

## Non-obvious rules worth remembering

- **Input coverage, not Cartesian product**: the pipeline calls the prediction API once per *distinct observed* feature tuple from the cleaned dataset (`drop_duplicates()`), not every possible combination. Only fall back to documented stratified sampling if that set is too large.
- **LLM is a narrator, not a calculator**: Python computes all statistics (medians, percentiles, sample sizes) first; Ollama only explains pre-computed facts and picks a chart type from an allowlist (`bar | horizontal_bar | line`). It must never be asked to invent numbers.
- **Secrets boundary**: `SUPABASE_SERVICE_ROLE_KEY` is pipeline/backend-only and must never be prefixed `VITE_` or bundled into the React app. The frontend uses only `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` under RLS (SELECT-only on published data, no anonymous writes).
- **Target leakage**: raw `salary` / `salary_currency` must not be used as predictors for `salary_in_usd` unless documented proof shows no leakage.
- **Error contracts**: FastAPI never returns raw stack traces; validation errors are 4xx with a stable JSON error shape (`{"error": {"code", "message", "details"}}`).
- **Model artifact trust**: `joblib`/pickle can execute code on load — only load artifacts produced by this project's own trusted pipeline, never accept uploaded or externally-fetched model files.
